export type CartItem = {
  id: string | number
  name: string
  price: number
  quantity: number
}

export type FormData = {
  name: string
  email: string
  address: string
  city: string
  zipCode: string
  country: string
} 